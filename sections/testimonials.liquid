{{ 'testimonial-sec.css' | asset_url | stylesheet_tag }}
{%- liquid
  # Spacing and alignment variables
  assign background_color = section.settings.background_color | default: '#222529'
  assign text_color = section.settings.text_color | default: '#ffffff'
  assign star_color = section.settings.star_color | default: '#eb5740'
  assign author_color = section.settings.author_color | default: '#a0a6ac'
  
  if section.settings.image != blank
    assign bg_image = section.settings.image | image_url: width: 1920
  endif
-%}

<div
  id="Testimonials-{{ section.id }}"
  class="testimonials-section spacing-style"
  style="
    --testimonials-bg-color: {{ background_color }};
    --testimonials-text-color: {{ text_color }};
    --testimonials-star-color: {{ star_color }};
    --testimonials-author-color: {{ author_color }};
    --testimonials-overlay-color: {{ section.settings.overlay_color | default: '#000000' }};
    --testimonials-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};
    --padding-block-start: {{ section.settings.padding-block-start }}px;
    --padding-block-end: {{ section.settings.padding-block-end }}px;
    {% if bg_image != blank %}
      background-image: url('{{ bg_image }}');
    {% endif %}
  ;"
  {% if request.visual_preview_mode %}
    data-shopify-visual-preview
  {% endif %}
>
  <!-- Background Overlay -->
  <div class="testimonials-section__overlay"></div>

  <!-- Content Container -->
  <div class="testimonials-section__container section section--page-width">
    {% if section.blocks.size > 0 %}
      <testimonials-slider 
        class="testimonials-slider" 
        data-autoplay="{{ section.settings.autoplay }}" 
        data-autoplay-speed="{{ section.settings.autoplay_speed | times: 1000 }}"
        data-total-slides="{{ section.blocks.size }}"
      >
        <!-- Slides List -->
        <div class="testimonials-slider__slides" ref="slidesContainer">
          {% for block in section.blocks %}
            <div 
              class="testimonials-slider__slide {% if forloop.first %}is-active{% endif %}" 
              data-index="{{ forloop.index0 }}"
              {{ block.shopify_attributes }}
            >
              <!-- Star Rating -->
              {% if block.settings.stars > 0 %}
                <div class="testimonials-slider__stars" aria-label="{{ block.settings.stars }} out of 5 stars">
                  {% for i in (1..block.settings.stars) %}
                   <svg width="15" height="15" viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">
<path d="M7.01786 0.863222C7.04707 0.804196 7.0922 0.75451 7.14816 0.719772C7.20411 0.685034 7.26866 0.666626 7.33452 0.666626C7.40038 0.666626 7.46493 0.685034 7.52089 0.719772C7.57684 0.75451 7.62197 0.804196 7.65119 0.863222L9.19119 3.98256C9.29264 4.18787 9.4424 4.36549 9.62761 4.50019C9.81281 4.63489 10.0279 4.72263 10.2545 4.75589L13.6985 5.25989C13.7638 5.26934 13.8251 5.29687 13.8755 5.33935C13.9259 5.38184 13.9635 5.43759 13.9839 5.50029C14.0043 5.563 14.0067 5.63016 13.9909 5.69418C13.9751 5.7582 13.9417 5.81652 13.8945 5.86256L11.4039 8.28789C11.2396 8.44796 11.1167 8.64554 11.0457 8.86364C10.9748 9.08174 10.9579 9.31382 10.9965 9.53989L11.5845 12.9666C11.596 13.0318 11.589 13.0989 11.5642 13.1603C11.5394 13.2218 11.4978 13.275 11.4442 13.3139C11.3906 13.3528 11.3272 13.3759 11.2611 13.3805C11.195 13.3851 11.129 13.371 11.0705 13.3399L7.99186 11.7212C7.789 11.6147 7.56331 11.5591 7.33419 11.5591C7.10507 11.5591 6.87938 11.6147 6.67652 11.7212L3.59852 13.3399C3.54008 13.3708 3.47412 13.3847 3.40816 13.3801C3.34219 13.3754 3.27887 13.3523 3.22539 13.3134C3.1719 13.2745 3.13041 13.2213 3.10563 13.16C3.08084 13.0987 3.07376 13.0317 3.08519 12.9666L3.67252 9.54056C3.71132 9.31438 3.69451 9.08215 3.62355 8.86392C3.55258 8.64569 3.4296 8.44799 3.26519 8.28789L0.774523 5.86322C0.726919 5.81724 0.693185 5.75882 0.677165 5.6946C0.661144 5.63039 0.663481 5.56297 0.683909 5.50002C0.704337 5.43706 0.742034 5.38112 0.792707 5.33854C0.843381 5.29597 0.904992 5.26849 0.970523 5.25922L4.41386 4.75589C4.64069 4.72289 4.85612 4.63526 5.04158 4.50055C5.22704 4.36583 5.37699 4.18807 5.47852 3.98256L7.01786 0.863222Z" fill="#E94F37" stroke="#E94F37" stroke-width="1.33333" stroke-linecap="round" stroke-linejoin="round"/>
</svg>

                  {% endfor %}
                </div>
              {% endif %}

              <!-- Testimonial Quote -->
              {% if block.settings.text != blank %}
                <blockquote class="testimonials-slider__quote">
                  <p class="testimonials-slider__quote-text">
                    {{ block.settings.text | escape }}
                  </p>
                </blockquote>
              {% endif %}

              <!-- Author -->
              {% if block.settings.author != blank %}
                <cite class="testimonials-slider__author">
                  - {{ block.settings.author | escape }}
                </cite>
              {% endif %}
            </div>
          {% endfor %}
        </div>

        <!-- Slider Progress Indicator (Bottom Navigation) -->
        {% if section.blocks.size > 1 %}
          <div class="testimonials-slider__navigation">
            <div class="testimonials-slider__progress-track" aria-label="Slideshow progress">
              <div 
                class="testimonials-slider__progress-bar" 
                ref="progressBar"
                style="width: calc(100% / {{ section.blocks.size }}); transform: translateX(0%);"
              ></div>
            </div>
          </div>
        {% endif %}
      </testimonials-slider>
    {% else %}
      <div class="testimonials-section__empty text-center">
        <p>Add testimonial blocks to showcase customer reviews.</p>
      </div>
    {% endif %}
  </div>
</div>

<style>
  .testimonials-section {
    position: relative;
    background-color: var(--testimonials-bg-color);
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    color: var(--testimonials-text-color);
    padding-block-start: var(--padding-block-start, 60px);
    padding-block-end: var(--padding-block-end, 60px);
    overflow: hidden;
  }

  .testimonials-section__overlay {
    position: absolute;
    inset: 0;
    background-color: var(--testimonials-overlay-color);
    opacity: var(--testimonials-overlay-opacity, 0.4);
    z-index: 1;
    pointer-events: none;
  }

  .testimonials-section__container {
    position: relative;
    z-index: 2;
    max-width: 800px;
    margin-inline: auto;
    text-align: center;
  }

  .testimonials-slider {
    display: block;
    width: 100%;
    position: relative;
  }

  .testimonials-slider__slides {
    position: relative;
    min-height: 180px;
    display: grid;
    grid-template-columns: 100%;
    grid-template-rows: auto;
  }

  .testimonials-slider__slide {
    grid-column: 1;
    grid-row: 1;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.25, 1, 0.5, 1), visibility 0.5s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .testimonials-slider__slide.is-active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    position: relative;
    z-index: 3;
  }

  .testimonials-slider__stars {
    display: flex;
    gap: 4px;
    margin-bottom: var(--padding-md, 20px);
    color: var(--testimonials-star-color);
  }

  .testimonials-slider__quote {
    margin: 0;
    padding: 0;
    margin-bottom: var(--padding-md, 20px);
    max-width: 640px;
  }

  .testimonials-slider__quote-text {
    font-size: clamp(14px, 2.5vw, 18px);
    line-height: 1.6;
    font-weight: 500;
    margin: 0;
  }

  .testimonials-slider__author {
    font-style: normal;
    font-size: 16px;
    text-transform:capitalize ;
    color:#FDFDFC;
    margin-bottom: 0;
    	font-family: var( --secondary-font);
  }
.testimonials-slider p.testimonials-slider__quote-text {
    font-size: 16px;
    color: #FDFDFC;
    font-weight: 500;
    letter-spacing: 0.5px;
}
  /* Progress Track Navigation */
  .testimonials-slider__navigation {
    display: flex;
    justify-content: center;
    margin-top: 0;
  }

  .testimonials-slider__progress-track {
    position: relative;
    width: 120px;
    height: 3px;
    background-color: rgba(255, 255, 255, 0.15);
    border-radius: 2px;
    overflow: hidden;
    cursor: pointer;
  }

  .testimonials-slider__progress-bar {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    background-color:#FDFDFC;
    border-radius: 2px;
    transform-origin: left center;
    transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1);
  }
</style>

<script>
  /**
   * TestimonialsSlider custom element for auto-rotation and swiping
   */
  class TestimonialsSlider extends HTMLElement {
    constructor() {
      super();
      this.currentSlide = 0;
      this.autoplayTimer = null;
      this.touchStartX = 0;
      this.touchEndX = 0;
    }

    connectedCallback() {
      this.totalSlides = parseInt(this.dataset.totalSlides) || 0;
      this.autoplay = this.dataset.autoplay === 'true';
      this.autoplaySpeed = parseInt(this.dataset.autoplaySpeed) || 5000;
      
      this.slides = this.querySelectorAll('.testimonials-slider__slide');
      this.progressBar = this.querySelector('.testimonials-slider__progress-bar');
      this.track = this.querySelector('.testimonials-slider__progress-track');

      if (this.totalSlides <= 1) return;

      // Start Autoplay
      this.startAutoplay();

      // Swipe support
      this.addEventListener('touchstart', (e) => {
        this.touchStartX = e.changedTouches[0].screenX;
      }, { passive: true });

      this.addEventListener('touchend', (e) => {
        this.touchEndX = e.changedTouches[0].screenX;
        this.handleSwipe();
      }, { passive: true });

      // Click to progress track to go to next slide
      if (this.track) {
        this.track.addEventListener('click', (e) => {
          const rect = this.track.getBoundingClientRect();
          const clickX = e.clientX - rect.left;
          const nextIndex = Math.floor((clickX / rect.width) * this.totalSlides);
          this.goToSlide(nextIndex);
        });
      }

      // Pause autoplay on mouse enter and resume on leave
      this.addEventListener('mouseenter', () => this.stopAutoplay());
      this.addEventListener('mouseleave', () => this.startAutoplay());
    }

    disconnectedCallback() {
      this.stopAutoplay();
    }

    startAutoplay() {
      if (!this.autoplay) return;
      this.stopAutoplay();
      this.autoplayTimer = setInterval(() => {
        this.nextSlide();
      }, this.autoplaySpeed);
    }

    stopAutoplay() {
      if (this.autoplayTimer) {
        clearInterval(this.autoplayTimer);
        this.autoplayTimer = null;
      }
    }

    nextSlide() {
      const nextIndex = (this.currentSlide + 1) % this.totalSlides;
      this.goToSlide(nextIndex);
    }

    prevSlide() {
      const prevIndex = (this.currentSlide - 1 + this.totalSlides) % this.totalSlides;
      this.goToSlide(prevIndex);
    }

    goToSlide(index) {
      if (index === this.currentSlide || index < 0 || index >= this.totalSlides) return;

      // Deactivate current slide
      this.slides[this.currentSlide].classList.remove('is-active');
      
      // Update index
      this.currentSlide = index;

      // Activate new slide
      this.slides[this.currentSlide].classList.add('is-active');

      // Update progress bar position
      if (this.progressBar) {
        this.progressBar.style.transform = `translateX(${this.currentSlide * 100}%)`;
      }
    }

    handleSwipe() {
      const swipeDistance = this.touchEndX - this.touchStartX;
      const swipeThreshold = 50; // pixels

      if (swipeDistance > swipeThreshold) {
        this.prevSlide();
        this.startAutoplay(); // Reset autoplay timer
      } else if (swipeDistance < -swipeThreshold) {
        this.nextSlide();
        this.startAutoplay(); // Reset autoplay timer
      }
    }
  }

  if (!customElements.get('testimonials-slider')) {
    customElements.define('testimonials-slider', TestimonialsSlider);
  }
</script>

{% schema %}
{
  "name": "Testimonials Slider",
  "tag": "section",
  "class": "testimonials-slider-section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Background Image"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#222529"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 50
    },
    {
      "type": "color",
      "id": "star_color",
      "label": "Star Rating Color",
      "default": "#eb5740"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Quote Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "author_color",
      "label": "Author Text Color",
      "default": "#a0a6ac"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Auto-Rotate Slides",
      "default": true
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "label": "Rotation Speed (Seconds)",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "default": 5
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Padding Top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Padding Bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "range",
          "id": "stars",
          "label": "Star Rating",
          "min": 0,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Quote",
          "default": "Your infinity FAA compliant stencils are absolutely wonderful. The facility notices the difference in the crisp lines and uniformity of the glass bead distribution from when we use the stencils versus when we use pads."
        },
        {
          "type": "text",
          "id": "author",
          "label": "Author / Source",
          "default": "Port Authority NY NJ"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials Slider",
      "category": "Text",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "text": "Your infinity FAA compliant stencils are absolutely wonderful. The facility notices the difference in the crisp lines and uniformity of the glass bead distribution from when we use the stencils versus when we use pads.",
            "author": "Port Authority NY NJ"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "text": "Excellent stencils that hold up to heavy use in airport runway marking projects. The alignment tabs make runway painting quick and accurate.",
            "author": "FAA Field Maintenance Crew"
          }
        }
      ]
    }
  ]
}
{% endschema %}
